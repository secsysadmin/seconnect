<style>
	p {
	  padding: 20px;
	  background-color: #D3D3D3; /* Red */
	  color: black;
	  margin-bottom: 15px;
	  font: serif;
	}
</style>

<div class="p-5 text-center bg-light">
    <h1 class="mb-3">Financial Forms</h1>
</div>

<p><%= link_to 'Reimbursement Request', requests_path(session[:user_id]) %> - View or Create a Reimbursement Request</p> <br>
<p><%= link_to 'Credit Card Request', creditcards_path(session[:user_id]) %> - View or Create a Credit Card Request</p> <br>
<p><%= link_to 'Approval To Charge', atcs_path(session[:user_id]) %> - View or Create an Approval To Charge Request</p> <br>
<p><%= link_to 'Vendors', vendors_path(session[:user_id]) %> - View or Create a Vendor</p> <br>
<p><%= link_to 'Forward Receipts', receipts_path(session[:user_id]) %> - Create and forward a receipt</p> <br>
<p><%= link_to 'Invoices', invoices_path(session[:admin_id]) %> - Create and forward a receipt</p> <br>


<% if User.find(session[:user_id]).permission_type == 'admin' %>
<%= link_to 'Home', admin_home_path(User.find(session[:user_id])) %>
<% else %>
<%= link_to 'Home', user_home_path(User.find(session[:user_id]))%>
<% end %>