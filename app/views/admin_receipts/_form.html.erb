<%= form_with(model: admin_receipt) do |form| %>
  <% if admin_receipt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_receipt.errors.count, "error") %> prohibited this admin_receipt from being saved:</h2>

      <ul>
        <% admin_receipt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
