<div class="page-wrapper">
    <div class="banner">
        <h1>New Committee</h1>
    </div>

    <%= render 'form', committee: @committee %>

    <% if @user.permission_type == 'admin' %>
        <div class="card-stack">
            <%= link_to 'Back', committees_path, {:class => "button"} %>
        </div>
    <% end %>
</div>